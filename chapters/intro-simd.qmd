# A primer on vectorization {#sec-intro-simd}

CPU performance scaled remarkably through the late 20^th^ century, driven by increasing transistor counts (roughly following the famous *Moore's law*) and rising clock frequencies.
However, as the clock frequencies started facing physical limits, CPU manufacturers invested in new solutions to keep improving the computation throughput.
The main trend over the last decades has been to focus on handling more operations in parallel, either by adding more CPU cores (*task-level* parallelism), or by increasing the amount of operations done by each core (*instruction-level* and *data-level* parallelism).
In particular, many applications benefit especially from data-level parallelism, where the same operation is applied *independently on multiple data* [@computer-architecture]: scientific computing, signal processing, regex engines [@hyperscan]...
In this chapter, we will present how vectorized instructions can speed up these use cases.

## A brief history of vectorized extensions

The concept of applying a single instruction to multiple data elements simultaneously (formally known as SIMD) predates the personal computer era.
The Cray-1 [@cray1], designed by Seymour Cray and introduced in 1976, was one of the first successful vector processors.
It exposed 8 vector registers of 64 double-precision floats each, with dedicated pipelined units that could operate on entire registers in a single instruction.
This made it particularly well-suited to the dense numerical workloads of scientific computing, but its vector model stayed confined to high-performance computing for the following decades.
The landscape shifted in 1996 when Intel introduced the MMX extension to consumer CPUs, motivated by the growing demands of multimedia workloads.
MMX exposed 64-bit registers holding multiple packed integers, establishing the template of the extensions that followed.
The main SIMD instruction set extensions available on modern x86 and ARM CPUs are summarized in @tbl-simd-x86 and @tbl-simd-arm respectively.

:::{#tbl-simd-x86 .large-table}
| Extension | Year | Width | Key operations | Adoption |
|:--:|:--:|:--:|:----------:|:--:|
| SSE | 1999 | 128 bits | Packed single-precision float | ~100% |
| SSE2 | 2001 | 128 bits | Packed double-precision float + integers | 98.09% |
| SSE3 | 2004 | 128 bits | Horizontal add, duplicate loads | 98.09% |
| SSSE3 | 2006 | 128 bits | Byte shuffle, abs, sign | 98.01% |
| SSE4.1 | 2007 | 128 bits | Blend, dot product | 97.97% |
| SSE4.2 | 2008 | 128 bits | String compare, `popcnt`, `lzcnt` | 97.93% |
| AVX | 2011 | 256 bits | 256-bit float operations | 96.93% |
| AVX2 | 2013 | 256 bits | 256-bit integers, permute, gather | 94.84% |
| AVX-512[^simd-avx512-subext] | 2016[^simd-avx512-year] | 512 bits | 512-bit float + int, mask registers, scatter | 21.89%[^simd-avx512-adoption] |

Main x86 SIMD instruction set extensions [@intel-manual], with desktop adoption from the Steam Hardware Survey [@steam-hwsurvey-2026]^[I was not expecting to cite Steam in my thesis, but here I am.].
:::

[^simd-avx512-subext]: AVX-512 is a family of sub-extensions. All modern implementations ship F (foundation), BW (byte/word operations), DQ (doubleword/quadword), and VL (128/256-bit variants of AVX-512 instructions) together. The Steam survey confirms identical adoption across all four.

[^simd-avx512-year]: First implemented in 2016 for servers and 2017 for desktop, then completely removed from Intel desktop processors in 2022 (Alder Lake and its successors). AMD implemented it in 2022 (Zen 4).

[^simd-avx512-adoption]: Apart from recent AMD CPUs, AVX-512 is only available for servers, thus explaining the low adoption for consumer CPUs.

:::{#tbl-simd-arm .large-table}
| Extension | Year | Width | Key operations | Adoption |
|:--:|:--:|:----:|:----------:|:---:|
| NEON | 2007 | 128 bits | Packed float + integers | All |
| SVE | 2016 | 128–2048 bits | Scalable vectors, predicated | HPC/server |
| SVE2 | 2019 | 128–2048 bits | Full-featured scalable vectors | HPC/server |

Main ARM SIMD instruction set extensions [@arm-manual].
:::

TBA

- brief history of modern CPU optimizations [@cray1]
- motivation: doing repeated operations, faster
- operations on wide registers
- new constraints: no cross-lane operations, no branches
- two classic examples: prefix sums and `memchr`
