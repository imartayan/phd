\usepackage{mathrsfs}
\usepackage{multirow}
\usepackage{pdfpages}
\usepackage{stmaryrd}
\usepackage{tikz}

\usetikzlibrary{arrows.meta,calc,chains,decorations.pathreplacing,matrix,patterns,patterns.meta,positioning,shapes,shapes.geometric,shapes.misc,shapes.multipart}

\AtBeginDocument{\hypersetup{bookmarksdepth=2}}

\AtBeginDocument{%
  \newtheoremstyle{plain}
  {\topsep}{\topsep}{\normalfont}{}{\bfseries}{.}{5pt plus 1pt minus 1pt}{}%
}

% Force all TOC entries to use the sans-serif font via tocbasic hooks,
% which fire around the actual entry rendering (after \normalfont resets)
\makeatletter
\BeforeStartingTOC[toc]{%
  \let\toc@@save@@normalfont\normalfont
  \renewcommand{\normalfont}{\toc@@save@@normalfont\sffamily\fontsize{11}{14}\selectfont}%
  \sffamily\fontsize{11}{14}\selectfont
}
\AfterStartingTOC[toc]{\let\normalfont\toc@@save@@normalfont}
\makeatother

\makeatletter
\newif\if@afterpart

% Remove the \newpage from the part ending, set flag instead
\renewcommand*{\partheadendvskip}{%
  \vskip\@tempskipa
  \global\@afterparttrue
}
\makeatother

\AddToHook{cmd/epigraph/after}{%
  \if@afterpart
    \vfill\clearpage
    \global\@afterpartfalse
  \fi
}
