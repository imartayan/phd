$if(has-frontmatter)$
\frontmatter
$endif$
$if(title)$
\pagenumbering{gobble}
\includepdf[pages=1]{titlepage/titlepage.pdf}
\cleardoublepage
$if(has-frontmatter)$
\pagenumbering{roman}
$else$
% \pagenumbering{arabic} would reset the counter to 1, so the printed
% number no longer matches the physical page; just switch the style.
\renewcommand*{\thepage}{\arabic{page}}
$endif$
$if(abstract)$
\begin{abstract}
$abstract$
\end{abstract}
$endif$
$endif$
